%h1 Listing districts

%table
  %tr
    %th Name
    %th Code
    %th
    %th
    %th

  - @districts.each do |district|
    %tr
      %td= district.name
      %td= district.code
      %td= link_to 'Show', district
      %td= link_to 'Edit', edit_district_path(district)
      %td= link_to 'Destroy', district, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New District', new_state_district_path(params[:state_id])
