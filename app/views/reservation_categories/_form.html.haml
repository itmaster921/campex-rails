= form_for @reservation_category do |f|
  - if @reservation_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@reservation_category.errors.count, "error")} prohibited this reservation_category from being saved:"
      %ul
        - @reservation_category.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
