%h1 Listing streams

%table
  %tr
    %th Name
    %th
    %th
    %th
    %th

  - @streams.each do |stream|
    %tr
      %td= stream.name
      %td= link_to 'Show', stream
      %td= link_to 'Edit', edit_stream_path(stream)
      %td= link_to 'Subjects', stream_stream_subjects_path(stream)
      %td= link_to 'Destroy', stream, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Stream', new_board_stream_path(params[:board_id])
